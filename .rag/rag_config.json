{
    "_comment": "RAG source configuration. Each source may provide `dir` (legacy) or `dirs` (array of path patterns). Use '**' for recursive globs or trailing '/' for non-recursive.",
    "skip_dirs": [
        ".git",
        "target",
        "node_modules",
        ".venv",
        "dist",
        "__pycache__"
    ],
    "sources": [
        {
            "dirs": ["./**"],
            "extensions": [
                ".md",
                ".py"
            ],
            "label": "codebase"
        }
    ]
}