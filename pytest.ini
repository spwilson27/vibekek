[pytest]
addopts = -x -n 4 -m "not slow"
timeout = 300
testpaths = tests
norecursedirs = rag-tool .venv __pycache__ .mypy_cache .pytest_cache
markers =
    slow: integration or end-to-end tests that are intentionally slower than the default unit-test budget
